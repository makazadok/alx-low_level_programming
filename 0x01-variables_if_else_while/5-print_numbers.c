#include <stdio.h>

/**
 * main - main function of a program that prints all single digit numbers
 * Return: be 0 for suceess execution
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
